namespace = poland_lithuania_events

# Owned by Polish/Lithuanian Radicals
poland_lithuania_events.1 = {
	type = country_event
	placement = ROOT

	title = poland_lithuania_events.1.t
	desc = poland_lithuania_events.1.d
	flavor = poland_lithuania_events.1.f

	event_image = {
		video = "europenorthamerica_springtime_of_nations"
	}

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	icon = "gfx/interface/icons/event_icons/event_flags/event_plc_flag.dds"

	duration = 3

	immediate = {
		capital = {
			save_scope_as = capital_scope
		}
		if = {
			limit = {
				capital = {
					state_region = {
						is_homeland = cu:polish
					}
				}
				NOT = { country_has_primary_culture = cu:polish }
			}
			add_primary_culture = cu:polish
		}
		if = {
			limit = {
				capital = {
					state_region = {
						is_homeland = cu:lithuanian
					}
				}
				NOT = { country_has_primary_culture = cu:lithuanian }
			}
			add_primary_culture = cu:lithuanian
		}
		set_variable = polish_revolter_var
		save_scope_as = polish_revolt_country
		if = {
			limit = { exists = c:POL }
			c:POL ?= { save_scope_as = poland_scope }
			ruler = {
				save_scope_as = poland_ruler_scope 
			}
		}
		else_if = {
			limit = { exists = c:PLC }
			c:PLC ?= { save_scope_as = poland_scope }
			ruler = {
				save_scope_as = poland_ruler_scope 
			}
		}
	}

	option = {
		name = poland_lithuania_events.1.a
		default_option = yes
		scope:poland_scope = {
			trigger_event = { id = poland_lithuania_events.2 popup = yes }
		}
	}
}

# Country gets taken over by Polish/Lithuanian nationalists and wants to join us
poland_lithuania_events.2 = {
	type = country_event
	placement = ROOT

	title = poland_lithuania_events.2.t
	desc = poland_lithuania_events.2.d
	flavor = poland_lithuania_events.2.f

	event_image = {
		video = "unspecific_signed_contract"
	}

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	icon = "gfx/interface/icons/event_icons/event_flags/event_plc_flag.dds"

	duration = 1

	trigger = {
		#Part of event chain
	}

	immediate = {
		ruler = {
			save_scope_as = poland_ruler_scope
		}
		random_country = {
			limit = {
				has_variable = polish_revolter_var
			}
			save_scope_as = polish_revolter
			capital = {
				save_scope_as = capital_scope
				set_variable = poland_steal_capital
			}
		}
		if = {
			limit = {
				scope:polish_revolter = { is_subject = yes }
			}
			scope:polish_revolter = { 
				overlord = { save_scope_as = polish_overlord_scope }
			}
		}
	}

	option = {
		name = poland_lithuania_events.2.a
		trigger = {
			scope:polish_revolter = {
				is_subject = no
			}
		}
		ai_chance = {
			base = 100
		}
		default_option = yes
		create_incident = {
			country = root
			value = 5
		}
		annex_with_incorporation = scope:polish_revolter
	}
	option = {
		name = poland_lithuania_events.2.b
		trigger = {
			scope:polish_revolter = {
				is_subject = no
			}
		}
		
		ai_chance = {
			base = 0
		}
		create_diplomatic_pact = {
			country = scope:polish_revolter
			type = puppet
		}
		scope:polish_revolter = {
			remove_variable = polish_revolter_var
		}
	}
	option = {
		name = poland_lithuania_events.2.c1
		trigger = {
			scope:polish_revolter = {
				is_subject = yes
			}
		}
		
		ai_chance = {
			base = 100
		}
		scope:polish_revolter = {
			every_scope_state = {
				state_region = {
					add_claim = scope:polish_overlord_scope
				}
			}
		}
		annex_with_incorporation = scope:polish_revolter
		create_incident = {
			country = root
			value = 5
		}
		
		scope:polish_overlord_scope = {
			trigger_event = { id = poland_lithuania_events.3 popup = yes }
		}
	}
	option = {
		name = poland_lithuania_events.2.d1
		trigger = {
			scope:polish_revolter = {
				is_subject = yes
			}
		}
		
		ai_chance = {
			base = 0
		}
		scope:polish_revolter = {
			every_scope_state = {
				state_region = {
					add_claim = scope:polish_overlord_scope
				}
			}
		}
		create_diplomatic_pact = {
			country = scope:polish_revolter
			type = puppet
		}
		scope:polish_revolter = {
			remove_variable = polish_revolter_var
		}
		
		scope:polish_overlord_scope = {
			trigger_event = { id = poland_lithuania_events.3 popup = yes }
			change_relations = { country = scope:polish_revolter value = -100 }
			hidden_effect = {
				set_secret_goal = {
					country = scope:polish_revolter
					secret_goal = conquer
				}
			}
		}
	}
	
	option = {
		name = poland_lithuania_events.2.e
		ai_chance = {
			base = 0
		}
		change_relations = { country = scope:polish_revolter value = -10 }
		scope:polish_revolter = {
			remove_variable = polish_revolter_var
		}
	}
}

# Polish nationalists takes over subject
poland_lithuania_events.3 = {
	type = country_event
	placement = ROOT

	title = poland_lithuania_events.3.t
	desc = poland_lithuania_events.3.d
	flavor = poland_lithuania_events.3.f

	event_image = {
		video = "europenorthamerica_political_extremism"
	}

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	icon = "gfx/interface/icons/event_icons/event_flags/event_plc_flag.dds"

	duration = 1

	trigger = {
		#Part of event chain
	}

	immediate = {
		if = {
			limit = { exists = c:POL }
			c:POL ?= { save_scope_as = poland_scope }
		}
		else_if = {
			limit = { exists = c:PLC }
			c:PLC ?= { save_scope_as = poland_scope }
		}
		random_state = {
			limit = { has_variable = poland_steal_capital }
			save_scope_as = capital_scope
		}
	}

	option = {
		name = poland_lithuania_events.3.a
		ai_chance = {
			base = 100
		}
		default_option = yes
		
		change_relations = { country = scope:poland_scope value = -100 }
		hidden_effect = {
			set_secret_goal = {
				country = scope:poland_scope
				secret_goal = conquer
			}
		}
	}
}

# Odrodzenie Startup
poland_lithuania_events.4 = {
	type = country_event
	placement = ROOT

	title = poland_lithuania_events.4.t
	desc = poland_lithuania_events.4.d
	flavor = {
		first_valid = {
			triggered_desc = {
				desc = poland_lithuania_events.4.f1
				trigger = {
					OR = {
						c:POL ?= this
						c:PLC ?= this
					}
				}
			}
			triggered_desc = {
				desc = poland_lithuania_events.4.f2
				trigger = {
					NOT = { 
						OR = {
							c:POL ?= this 
							c:PLC ?= this 
						}
						capital = {
							state_region = {
								is_homeland = cu:polish
							}
						}
					}
				}
			}
			triggered_desc = {
				desc = poland_lithuania_events.4.f2
				trigger = {
					NOT = { 
						OR = {
							c:POL ?= this 
							c:PLC ?= this 
						}
						capital = {
							state_region = {
								is_homeland = cu:lithuanian
							}
						}
					}
				}
			}
		}
	}

	event_image = {
		video = "votp_agitator_speaking"
	}

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	icon = "gfx/interface/icons/event_icons/event_flags/event_plc_flag.dds"

	duration = 1

	trigger = {
		# Fired by JE startup
	}
	
	immediate = {
		if = {
			limit = { exists = c:POL }
			c:POL ?= { save_scope_as = poland_scope }
			ruler = {
				save_scope_as = poland_ruler_scope 
			}
		}
		else_if = {
			limit = { exists = c:PLC }
			c:PLC ?= { save_scope_as = poland_scope }
			ruler = {
				save_scope_as = poland_ruler_scope 
			}
		}
		if = {
			limit = {
				is_subject = yes
			}
			overlord = { save_scope_as = polish_overlord_scope }
		}
	}

	option = {
		name = poland_lithuania_events.4.a
		default_option = yes
		trigger = {
			scope:poland_scope ?= this
		}
		every_interest_group = {
			limit = {
				OR = {
					has_ideology = ideology:ideology_patriotic
					has_ideology = ideology:ideology_nationalist
					leader = {
						OR = {						
							has_ideology = ideology:ideology_radical
							has_ideology = ideology:ideology_jingoist_leader
							has_ideology = ideology:ideology_ethno_nationalist
						}
					}
				}
			}
			root = {
				add_loyalists = {
					value = medium_radicals
					culture = cu:polish
					interest_group = prev
				}
			}
			add_modifier = {
				name = modifier_odrodzenie_supporters
				months = normal_modifier_time
			}
		}
		add_modifier = {
			name = modifier_odrodzenie_support
			months = long_modifier_time
			is_decaying = yes
		}
		every_state = {
			limit = {
				state_region = {
					OR = {
						is_homeland = cu:polish
						is_homeland = cu:lithuanian
					}
				}
				NOT = { has_claim_by = root }
				NOT = { owner = root }
				NOT = { owner = { is_subject_of = root } }
			}
			state_region = {
				add_claim = root
			}
		}
		every_country = {
			limit = {
				has_interest_marker_in_region = region_poland
				NOT = {
					THIS = ROOT
				}
			}
			post_notification = pol_odrodzenie_intent
		}
		if = {
			limit = {
				is_ai = yes
			}
			set_strategy = ai_strategy_pol_odrodzenie
		}
	}
	option = {
		name = poland_lithuania_events.4.b
		default_option = yes
		trigger = {
			NOT = { scope:poland_scope ?= this }
		}
	}
}

# Union of Augustów complete
poland_lithuania_events.5 = {
	type = country_event
	placement = ROOT

	title = poland_lithuania_events.5.t
	desc = poland_lithuania_events.5.d
	flavor = poland_lithuania_events.5.f

	event_image = {
		video = "unspecific_military_parade"
	}

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	icon = "gfx/interface/icons/event_icons/event_flags/event_plc_flag.dds"

	duration = 1

	trigger = {
		#Part of event chain
	}

	immediate = {
		c:POL ?= { 
			save_scope_as = pol_scope 
			ruler = { 
				save_scope_as = poland_ruler_scope 
				interest_group = { save_scope_as = ruler_ig_scope }
			}
		}
		c:LIT ?= { save_scope_as = lit_scope }
	}
	
	option = {
		name = poland_lithuania_events.5.a
		default_option = yes
		annex_with_incorporation = c:LIT
		change_tag = PLC
		
		hidden_effect = {
			if = {
				limit = { NOT = { country_has_primary_culture = cu:lithuanian } }
				add_primary_culture = cu:lithuanian
			}
		}
		
		# prestige
		add_modifier = {
			name = plc_unifier_prestige
			months = long_modifier_time
		}
	}
	option = {
		name = poland_lithuania_events.5.b
		annex_with_incorporation = c:LIT
		change_tag = PLC
		
		hidden_effect = {
			if = {
				limit = { NOT = { country_has_primary_culture = cu:lithuanian } }
				add_primary_culture = cu:lithuanian
			}
		}
		
		# ruler IG attraction
		scope:poland_ruler_scope = {
			add_modifier = {
				name = plc_unifiers_ruler
				months = -1
			}
		}
		scope:ruler_ig_scope = {
			add_modifier = {
				name = plc_unifiers_ig
				months = long_modifier_time
			}
		}
	}
}

# Union of Augustów fail
poland_lithuania_events.6 = {
	type = country_event
	placement = ROOT

	title = poland_lithuania_events.6.t
	desc = poland_lithuania_events.6.d
	flavor = poland_lithuania_events.6.f

	event_image = {
		video = "unspecific_politicians_arguing"
	}

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	icon = "gfx/interface/icons/event_icons/event_skull.dds"

	duration = 1

	trigger = {
		#Part of event chain
	}

	immediate = {
		c:POL ?= { save_scope_as = pol_scope }
		c:LIT ?= { save_scope_as = lit_scope }
	}
	
	option = {
		name = poland_lithuania_events.6.a
		default_option = yes
		
		if = {
			limit = { c:POL ?= this }
			change_relations = { country = c:LIT value = -100 }
			c:LIT ?= {
				every_scope_state = {
					state_region = {
						add_claim = root
					}
				}
			}
			hidden_effect = {
				set_secret_goal = {
					country = c:LIT
					secret_goal = conquer
				}
			}
		}
		
		else_if = {
			limit = { c:LIT ?= this }
			show_as_tooltip = { change_relations = { country = c:POL value = -100 } }
			c:POL ?= {
				every_scope_state = {
					state_region = {
						add_claim = root
					}
				}
			}
			hidden_effect = {
				set_secret_goal = {
					country = c:POL
					secret_goal = antagonize
				}
			}
		}
	}
}