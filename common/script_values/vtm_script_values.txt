limited_construction_threshold = 200
very_limited_construction_threshold = 100

num_active_colonies = {
	value = {
		value = 0
		if = {
			limit = {
				has_colonial_growth = yes
			}
			every_scope_state = {
				limit = { 
					is_under_colonization = yes
				}
				add = 1
			}
		}
    }
}

state_region_num_provinces_half = {
	value = {
		every_state = {
			limit = {
				state_region = root.state_region
			}
			add = num_provinces
		}
		multiply = 0.5
		round = yes
	}
}

state_region_num_provinces_fifth = {
	value = {
		every_state = {
			limit = {
				state_region = root.state_region
			}
			add = num_provinces
		}
		multiply = 0.3
		round = yes
	}
}

# used in loc
spain_state_effect_authority_bonus = {
	value = root.authority
	divide = 1000
	min = 0
}
