law_serfdom = {
	group = lawgroup_land_reform
	
	icon = "gfx/interface/icons/law_icons/serfdom.dds"
	
	progressiveness = -50
	
	disallowing_laws = {
		law_interventionism
		law_laissez_faire
		law_command_economy
	}
	
	on_enact = {
		if = {
			limit = {
				OR = {
					has_law = law_type:law_commercialized_agriculture
					has_law = law_type:law_homesteading
				}
			}
			custom_tooltip = farmers_pb_ig_shift_effect_negative_tt
		}
		if = {
			limit = {
				OR = {
					has_law = law_type:law_commercialized_agriculture
					has_law = law_type:law_collectivized_agriculture
				}
			}
			custom_tooltip = rural_laborers_tu_ig_shift_effect_negative_tt
		}
	}
	
	on_activate = {
		if = {
			limit = {
				c:PER ?= this
				any_scope_state = {
					state_region = { is_homeland = cu:persian }
					NOT = { has_modifier = landowners_qanat_maintenance }
				}
			}
			custom_tooltip = {
				text = landowners_qanat_maintenance_gain_tt
				every_scope_state = {
					limit = {
						state_region = { is_homeland = cu:persian }
					}
					add_modifier = {
						name = landowners_qanat_maintenance
						months = -1
					}
				}
			}
		}
	}
	
	modifier = {
		interest_group_ig_landowners_pol_str_mult = 0.5
		state_infrastructure_from_population_max_mult = -0.2
	}

	possible_political_movements = {
		law_tenant_farmers
	}

	pop_support = {
		value = 0
		add = {
			desc = "POP_ARISTOCRATS"			
			if = {
				limit = {
					is_pop_type = aristocrats
				}
				value = 0.1
			}
		}
	}

	# AI should never try to bring back serfdom
	ai_will_do = {
		always = no
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = {
				law_is_important = yes
			}
			add = 750
		}
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_tenant_farmers = {
	group = lawgroup_land_reform
	
	icon = "gfx/interface/icons/law_icons/tenant_farmers.dds"
	
	progressiveness = 0

	modifier = {
		interest_group_ig_landowners_pol_str_mult = 0.25
	}

	possible_political_movements = {
		law_homesteading
		law_collectivized_agriculture
		law_commercialized_agriculture
	}
	
	on_enact = {
		if = {
			limit = {
				OR = {
					has_law = law_type:law_commercialized_agriculture
					has_law = law_type:law_homesteading
				}
			}
			custom_tooltip = farmers_pb_ig_shift_effect_negative_tt
		}
		if = {
			limit = {
				OR = {
					has_law = law_type:law_commercialized_agriculture
					has_law = law_type:law_collectivized_agriculture
				}
			}
			custom_tooltip = rural_laborers_tu_ig_shift_effect_negative_tt
		}
	}
	
	on_activate = {
		if = {
			limit = {
				c:PER ?= this
				any_scope_state = {
					state_region = { is_homeland = cu:persian }
					NOT = { has_modifier = landowners_qanat_maintenance }
				}
			}
			custom_tooltip = {
				text = landowners_qanat_maintenance_gain_tt
				every_scope_state = {
					limit = {
						state_region = { is_homeland = cu:persian }
					}
					add_modifier = {
						name = landowners_qanat_maintenance
						months = -1
					}
				}
			}
		}
		if = {
			limit = {
				NOT = { has_law = law_type:law_feudalism }
				ig:ig_landowners = {
					has_ideology = ideology:ideology_stratocratic
				}
			}
			ig:ig_landowners = {
				remove_ideology = ideology_stratocratic
				add_ideology = ideology_modern_stratocratic
			}
		}
	}

	pop_support = {
		value = 0
		add = {
			desc = "POP_ARISTOCRATS"			
			if = {
				limit = {
					owner = {
						NOT = { has_law = law_type:law_serfdom }
					}
					is_pop_type = aristocrats
				}
				value = 0.1
			}
		}
		add = {
			desc = "POP_PEASANTS"		
			if = {
				limit = { 
					is_pop_type = peasants
					owner = { 
						has_law = law_type:law_serfdom
					}
				}
				add = {
					value = literacy_rate
					divide = 2
				}
			}			
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = {
				law_is_important = yes
			}
			add = 750
		}
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_commercialized_agriculture = {
	group = lawgroup_land_reform
	
	icon = "gfx/interface/icons/law_icons/commercial_agriculture.dds"
	
	progressiveness = 0
	
	can_enact = {
		OR = {
			has_law = law_type:law_tenant_farmers
			has_law = law_type:law_collectivized_agriculture
			has_law = law_type:law_homesteading
		}
	}

	disallowing_laws = {
		law_traditionalism
		law_feudalism
	}

	modifier = {
		state_farmers_investment_pool_efficiency_mult = 0.1
	}

	possible_political_movements = {
		law_tenant_farmers
		law_homesteading
		law_collectivized_agriculture
	}

	unlocking_technologies = {
		mutual_funds
	}
	
	on_enact = {
		if = {
			limit = {
				NOR = {
					has_law = law_type:law_commercialized_agriculture
					has_law = law_type:law_homesteading
				}
			}
			custom_tooltip = farmers_pb_ig_shift_effect_tt
		}
		if = {
			limit = {
				NOR = {
					has_law = law_type:law_commercialized_agriculture
					has_law = law_type:law_collectivized_agriculture
				}
			}
			custom_tooltip = rural_laborers_tu_ig_shift_effect_tt
		}
	}
	
	on_activate = {
		add_modifier = {
			name = land_redistribution_modifier
			months = long_modifier_time
			is_decaying = yes
		}
		add_modifier = {
			name = land_redistribution_compensation_modifier
			months = long_modifier_time
			is_decaying = yes
			multiplier = money_amount_multiplier_small
		}
		if = {
			limit = {
				NOT = { has_law = law_type:law_feudalism }
				ig:ig_landowners = {
					has_ideology = ideology:ideology_stratocratic
				}
			}
			ig:ig_landowners = {
				remove_ideology = ideology_stratocratic
				add_ideology = ideology_modern_stratocratic
			}
		}
		if = {
			limit = {
				c:PER ?= this
				any_scope_state = {
					has_modifier = landowners_qanat_maintenance
				}
			}
			custom_tooltip = {
				text = landowners_qanat_maintenance_loss_tt
				every_scope_state = {
					limit = { has_modifier = landowners_qanat_maintenance }
					remove_modifier = landowners_qanat_maintenance
				}
			}
		}
	}

	pop_support = {
		value = 0

		add = {
			desc = "POP_CAPITALISTS"			
			if = {
				limit = {
					is_pop_type = capitalists
				}
				value = 0.1
			}
		}

		add = {
			desc = "POP_PEASANTS"		
			if = {
				limit = { 
					is_pop_type = peasants
					owner = { 
						has_law = law_type:law_serfdom
					}
				}
				add = {
					value = literacy_rate
					divide = 2
				}
			}			
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = {
				law_is_important = yes
			}
			add = 750
		}
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_homesteading = {
	group = lawgroup_land_reform
	
	icon = "gfx/interface/icons/law_icons/freeholders.dds"
	
	progressiveness = 25
	
	can_enact = {
		OR = {
			has_law = law_type:law_tenant_farmers
			has_law = law_type:law_collectivized_agriculture
			has_law = law_type:law_commercialized_agriculture
		}
	}
	
	disallowing_laws = {
		law_traditionalism
		law_feudalism
	}

	possible_political_movements = {
		law_tenant_farmers
		law_collectivized_agriculture
		law_commercialized_agriculture
	}

	modifier = {
		country_farmers_pol_str_mult = 0.10
		state_farmers_investment_pool_efficiency_mult = 0.25
	}

	on_enact = {
		if = {
			limit = {
				NOR = {
					has_law = law_type:law_commercialized_agriculture
					has_law = law_type:law_homesteading
				}
			}
			custom_tooltip = farmers_pb_ig_shift_effect_tt
		}
		if = {
			limit = {
				OR = {
					has_law = law_type:law_commercialized_agriculture
					has_law = law_type:law_collectivized_agriculture
				}
			}
			custom_tooltip = rural_laborers_tu_ig_shift_effect_negative_tt
		}
	}
	
	on_activate = {
		add_modifier = {
			name = land_redistribution_modifier
			months = long_modifier_time
			is_decaying = yes
		}
		add_modifier = {
			name = land_redistribution_compensation_modifier
			months = long_modifier_time
			is_decaying = yes
			multiplier = money_amount_multiplier_small
		}
		if = {
			limit = {
				NOT = { has_law = law_type:law_feudalism }
				ig:ig_landowners = {
					has_ideology = ideology:ideology_stratocratic
				}
			}
			ig:ig_landowners = {
				remove_ideology = ideology_stratocratic
				add_ideology = ideology_modern_stratocratic
			}
		}
		if = {
			limit = {
				c:PER ?= this
				any_scope_state = {
					has_modifier = landowners_qanat_maintenance
				}
			}
			custom_tooltip = {
				text = landowners_qanat_maintenance_loss_tt
				every_scope_state = {
					limit = { has_modifier = landowners_qanat_maintenance }
					remove_modifier = landowners_qanat_maintenance
				}
			}
		}
	}

	pop_support = {
		value = 0

		add = {
			desc = "POP_FARMERS"			
			if = {
				limit = {
					is_pop_type = farmers
				}
				value = 0.50
			}
		}

		add = {
			desc = "POP_PEASANTS"		
			if = {
				limit = { 
					is_pop_type = peasants
					owner = { 
						OR = {
							has_law = law_type:law_serfdom
							has_law = law_type:law_tenant_farmers
						}
					}
				}
				add = {
					value = literacy_rate
				}
			}			
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				law_is_important = yes
			}
			add = 750
		}
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}

law_collectivized_agriculture = {
	group = lawgroup_land_reform
	
	icon = "gfx/interface/icons/law_icons/collectivized_agriculture.dds"
	
	progressiveness = 25
	
	disallowing_laws = {
		law_feudalism
	}
	
	can_enact = {
		OR = {
			has_law = law_type:law_tenant_farmers
			has_law = law_type:law_commercialized_agriculture
			has_law = law_type:law_homesteading
		}
	}

	on_enact = {
		custom_tooltip = rural_laborers_tu_ig_shift_effect_tt
		if = {
			limit = {
				OR = {
					has_law = law_type:law_commercialized_agriculture
					has_law = law_type:law_homesteading
				}
			}
			custom_tooltip = farmers_pb_ig_shift_effect_negative_tt
		}
	}
	
	on_activate = {
		if = {
			limit = {
				NOT = { has_law = law_type:law_feudalism }
				ig:ig_landowners = {
					has_ideology = ideology:ideology_stratocratic
				}
			}
			ig:ig_landowners = {
				remove_ideology = ideology_stratocratic
				add_ideology = ideology_modern_stratocratic
			}
		}
		if = {
			limit = {
				c:PER ?= this
				any_scope_state = {
					has_modifier = landowners_qanat_maintenance
				}
			}
			custom_tooltip = {
				text = landowners_qanat_maintenance_loss_tt
				every_scope_state = {
					limit = { has_modifier = landowners_qanat_maintenance }
					remove_modifier = landowners_qanat_maintenance
				}
			}
		}
	}

	unlocking_laws = {
		law_command_economy
		law_cooperative_ownership
	}

	possible_political_movements = {
		law_tenant_farmers
		law_homesteading
		law_commercialized_agriculture
	}

	on_activate = {
		add_modifier = {
			name = land_redistribution_modifier
			months = long_modifier_time
			is_decaying = yes
		}
		## Red Sun in the Sky starts playing ##
	}

	unlocking_technologies = { socialism }

	pop_support = {
		value = 0

		add = {
			desc = "POP_PEASANTS"		
			if = {
				limit = { 
					is_pop_type = peasants
					owner = { 
						has_law = law_type:law_serfdom
					}
				}
				add = {
					value = literacy_rate
				}
			}			
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = {
				law_is_important = yes
			}
			add = 750
		}
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
		
		if = {
			limit = { 
				has_law = law_type:law_council_republic
			}
			add = 250
		}
	}
}
