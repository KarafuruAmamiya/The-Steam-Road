GER = {
	states = {
		STATE_ANHALT
		STATE_RHINELAND
		STATE_NORTH_RHINE
		STATE_BRUNSWICK
		STATE_SAXONY
		STATE_HESSE
		STATE_THURINGIA
		STATE_WURTTEMBERG
		STATE_BADEN
		STATE_FRANCONIA
		STATE_AUSTRIA
		STATE_PFALZ
		STATE_OBERPFALZ
		STATE_ELSASS
	}

	required_states_fraction = 1
	
	is_major_formation = yes
	
	unification_play = dp_unify_germany
	leadership_play = dp_leadership_germany
	
	ai_will_do = { has_technology_researched = pan_nationalism }

	possible = {
		has_technology_researched = pan-nationalism
		custom_tooltip = {
			text = je_german_unification_idea_trigger_desc
			has_variable = je_german_unification_idea
		}
		custom_tooltip = {
			text = hre_doesnt_exist
			NOT = {
				OR = {
					exists = c:HRE
					AND = {
						exists = c:SAX
						c:SAX = { has_modifier = hre_emperor }
					}
				}
			}
		}

	}
}
