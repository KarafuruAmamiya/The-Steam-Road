# Root = Country
can_lead_power_bloc = {
	OR = {
		country_rank >= rank_value:major_power
		country_rank = rank_value:unrecognized_major_power
	}
	trigger_if = {
        limit = {
            power_bloc ?= {
                has_identity = identity:identity_hre
            }
        }
        NOT = {
            has_global_variable = hre_end
        }
    }
}